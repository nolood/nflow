# Ralph Progress Log
Started: Wed Feb 11 06:57:15 PM MSK 2026
---

## Codebase Patterns
- Handler pattern: extract params → validate UUID → open DB → get run → validate state → update DB → emit event → return response
- `update_pipeline_run()` takes string status values (e.g., "running", "completed") not enum variants — use the string literals directly
- Event bus is `Option<SharedEventBus>` — always check with `if let Some(bus) = &state.event_bus`
- Pre-existing warnings exist in clippy/build — don't try to fix them unless they're in your changed code
- Pipeline question DB functions: insert_question(), get_pending_questions(pipeline_run_id), answer_question(id, answer, answered_by) — answer_question returns bool
- `get_pending_questions()` filters `answered = 0` in SQL, so returns only unanswered questions
- The daemon lock test (`test_acquire_lock_contention`) is flaky when daemon is running — pre-existing issue
- `HandlerState` has `pipeline_signals: PipelineSignals` field — must be initialized with `Arc::new(Mutex::new(HashMap::new()))` in all constructors/tests
- For async signaling between handlers and executor tasks, use `tokio::sync::watch` channels keyed by pipeline_run_id in the shared `pipeline_signals` map
- When updating test files that construct `HandlerState`, all 5 test modules (protocol, e2e, commands_exec, commands_plan, commands_project_spec) must be updated
- Auto-answerer uses `extract_pipeline_json_from_text()` (same as stage output parsing) to extract JSON from Claude's response
- `RunConfig::for_pipeline_auto_answer` limits to Read/Glob/Grep with max_turns=10 — no write/edit/bash access
- `auto_answer_question()` in handlers.rs is the full-flow function: spawn agent → parse → save to DB → emit event
- `extract_pipeline_questions()` parses the PIPELINE_QUESTIONS marker + ```json block from Plan agent output; returns (questions, cleaned_text)
- `run_pipeline_stage()` returns a 3-tuple: `(StageOutput, Option<String>, String)` — the third element is raw result_text for question detection
- For Plan stages, `run_pipeline_stage` strips PIPELINE_QUESTIONS from text before parsing plan JSON to avoid picking up questions as plan output
- Question routing: Auto mode spawns parallel auto-answerer tasks (tokio::spawn), Manual mode polls DB every 500ms until all questions answered

---

## 2026-02-11 - US-006
- Implemented `pipeline.approve` and `pipeline.reject` command handlers in handlers.rs
- Added both handlers to the command dispatch table
- `pipeline.approve`: handles WaitingForApproval (→ Running/Implement) and WaitingForFinalApproval (→ Completed)
- `pipeline.reject`: handles WaitingForApproval (→ restart Plan with feedback) and WaitingForFinalApproval (→ increment iteration, back to Implement)
- Reject enforces max_iterations check — marks Failed when exceeded
- Both handlers emit appropriate events (PipelinePlanApproved/Rejected, PipelineFinalApproved/Rejected)
- Files changed: crates/nflow-daemon/src/handlers.rs
- **Learnings for future iterations:**
  - Handler pattern: extract params → validate UUID → open DB → get run → validate state → update DB → emit event → return response
  - `update_pipeline_run()` takes string status values (e.g., "running", "completed") not enum variants — use the string literals directly
  - Event bus is `Option<SharedEventBus>` — always check with `if let Some(bus) = &state.event_bus`
  - Pre-existing warnings exist in clippy/build — don't try to fix them unless they're in your changed code
  - The daemon lock test (`test_acquire_lock_contention`) is flaky when daemon is running — it's a pre-existing issue
---

## 2026-02-11 - US-007
- US-007 was already implemented in commit 17112ab — `pipeline.questions` handler in handlers.rs
- Handler accepts `{ pipeline_run_id }`, calls `db::pipeline::get_pending_questions()`, returns `{ questions: [{ id, question, context }] }`
- Dispatch route at line 81 in handlers.rs
- Handler implementation at lines 7374-7422
- Verified `cargo build --workspace` succeeds
- Marked `passes: true` in prd.json
- Files changed: scripts/ralph/prd.json (marked passes), scripts/ralph/progress.txt (added patterns + this entry)
- **Learnings for future iterations:**
  - Check if handlers were already committed before implementing — git log shows commit history
  - The `pipeline.questions` handler is simpler than others — no state validation needed since listing is read-only
---

## 2026-02-11 - US-008
- Implemented pipeline executor manual mode approval gates in handlers.rs
- Added `PipelineApprovalSignal` enum (None, Approved, Rejected{feedback}) and `PipelineSignals` type alias
- Added `pipeline_signals: PipelineSignals` field to `HandlerState` — shared map of `tokio::sync::watch::Sender<PipelineApprovalSignal>` keyed by pipeline_run_id
- **Plan approval gate**: After Plan stage in manual mode, executor updates DB to `waiting_for_approval`, emits `PipelinePlanReady`, creates watch channel in shared map, and waits for signal. On `Approved` → proceeds to Implement. On `Rejected` → continues loop to re-run Plan.
- **Final approval gate**: After Review passes in manual mode, executor updates DB to `waiting_for_final_approval`, emits `PipelineFinalApprovalReady`, creates watch channel, waits. On `Approved` → marks completed, returns. On `Rejected` → checks if pipeline is failed (max_iterations), otherwise loops back.
- Updated `handle_pipeline_approve` to send `PipelineApprovalSignal::Approved` via watch channel after DB update
- Updated `handle_pipeline_reject` to send `PipelineApprovalSignal::Rejected` via watch channel after DB update
- Updated `handle_pipeline_start` to parse `mode` param from request and pass actual mode to executor task
- Updated all `PipelineRun` constructions in executor to use actual `mode` instead of hardcoded `PipelineMode::Auto`
- Updated `HandlerState` in main.rs and all 5 test files to include `pipeline_signals` field
- Files changed: handlers.rs, main.rs, protocol_tests.rs, e2e_tests.rs, commands_exec_tests.rs, commands_plan_tests.rs, commands_project_spec_tests.rs
- All 554 daemon tests pass, all 152 non-daemon tests pass
- `cargo build --workspace` succeeds, `cargo clippy --workspace --all-targets` passes (no new warnings)
- **Learnings for future iterations:**
  - `HandlerState` has `pipeline_signals: PipelineSignals` field — must be initialized with `Arc::new(Mutex::new(HashMap::new()))` in all constructors/tests
  - Use `tokio::sync::watch` for signaling between handlers and executor tasks — it supports carrying data (approve/reject/feedback) unlike plain Notify
  - When updating `HandlerState`, must update 7 locations: main.rs, handlers.rs (struct def + 6 test constructions), plus 5 external test files
  - Signal channels are inserted into the shared map just before waiting, and removed after signal is received — prevents stale entries
  - For final rejection with max_iterations exceeded, the executor must check DB status after receiving rejection signal to detect if pipeline was marked as failed
---

## 2026-02-11 - US-009
- Updated `prompts/pipeline_plan.md` to add structured question output format
- Added 3-step workflow: (1) Explore codebase, (2) Ask questions with PIPELINE_QUESTIONS marker, (3) Create the plan
- Questions format: PIPELINE_QUESTIONS marker followed by ```json block with `{ "questions": [{ "question": "...", "context": "..." }] }`
- If no questions needed, agent skips the questions block entirely
- Prompts are embedded at compile time via `include_str!` in `nflow-claude/src/prompt.rs`
- `cargo build --workspace` succeeds (nflow-claude recompiled)
- Files changed: prompts/pipeline_plan.md
- **Learnings for future iterations:**
  - Prompts are embedded at compile time — changing a prompt file triggers recompilation of nflow-claude crate
  - The PIPELINE_QUESTIONS marker is what the daemon will use to detect and parse questions from stream output (US-011 will implement the parser)
  - Keep prompt changes backward-compatible — the plan JSON output format at the end is unchanged
---

## 2026-02-11 - US-010
- Created `prompts/pipeline_auto_answer.md` prompt template with `{question}` and `{context}` variables
- Prompt instructs agent to search codebase with Read/Glob/Grep and output structured JSON: `{ "answer": "...", "confidence": "high|medium|low", "sources": ["..."] }`
- Added `RunConfig::for_pipeline_auto_answer` in `nflow-claude/src/runner.rs` — allowedTools: Read,Glob,Grep; max_turns: 10
- Registered `pipeline_auto_answer` template in `nflow-claude/src/prompt.rs` (embedded at compile time)
- Added auto-answerer spawning logic in `nflow-daemon/src/handlers.rs`:
  - `AutoAnswerOutput` struct for parsing agent response JSON
  - `build_auto_answer_prompt()` — loads and renders the auto_answer template
  - `spawn_auto_answerer()` — spawns Claude process with 60-second timeout, parses JSON output, falls back to default answer
  - `auto_answer_question()` — full-flow function: spawn agent → parse → save to DB via `answer_question()` with answered_by='auto-agent' → emit `PipelineQuestionAnswered` event
- Added tests: prompt template existence test, RunConfig tools/limits tests
- Files changed: prompts/pipeline_auto_answer.md, crates/nflow-claude/src/prompt.rs, crates/nflow-claude/src/runner.rs, crates/nflow-daemon/src/handlers.rs
- All tests pass: 554 daemon, 782+ non-daemon
- `cargo build --workspace` succeeds, `cargo clippy --workspace --all-targets` passes (no new warnings)
- **Learnings for future iterations:**
  - `extract_pipeline_json_from_text()` is reusable for any agent output that embeds JSON — auto-answerer uses it too
  - The `auto_answer_question()` function is designed to be called from US-011's question detection/routing logic
  - Auto-answerer fallback is intentionally generous — if raw text is short enough (<2000 chars), it's used as the answer; otherwise the static fallback is used
  - Note: agent_runs record creation for auto-answerer is deferred to US-011 where the actual invocation context (pipeline_run_id, etc.) is available in the executor loop
---

## 2026-02-11 - US-011
- Implemented question detection and routing from Plan agent stream output in the pipeline executor
- Added `PipelineQuestionsBlock` and `PipelineQuestionItem` structs for deserializing the PIPELINE_QUESTIONS JSON
- Added `extract_pipeline_questions()` function: parses PIPELINE_QUESTIONS marker + ```json block, returns (questions, cleaned_text)
- Modified `run_pipeline_stage()` return type to include raw `result_text` as third tuple element for caller-side question detection
- In `run_pipeline_stage()`, Plan stages now strip PIPELINE_QUESTIONS section before parsing plan JSON
- In executor loop (after Plan stage, before approval gate):
  - Detects questions from `plan_result_text` using `extract_pipeline_questions()`
  - Inserts each question into DB via `insert_question()`
  - Emits `PipelineQuestion` events for each question
  - Sends `pipeline_question` streaming response lines to client
  - **Auto mode**: Spawns parallel `auto_answer_question()` tasks via `tokio::spawn`, waits for all to complete
  - **Manual mode**: Sends `waiting_for_answers` message, polls DB every 500ms until all questions are answered
  - After all questions answered (either mode): sends `questions_answered` notification
  - If no questions detected, proceeds directly to plan completion (no-op)
- Added 4 unit tests: `test_extract_pipeline_questions_with_marker`, `_no_marker`, `_invalid_json`, `_empty_array`
- Files changed: crates/nflow-daemon/src/handlers.rs, scripts/ralph/prd.json, scripts/ralph/progress.txt
- All tests pass: 558 daemon (554 existing + 4 new), 81 non-daemon (1 pre-existing flaky failure)
- `cargo build --workspace` succeeds, `cargo clippy --workspace --all-targets` passes (no new warnings)
- **Learnings for future iterations:**
  - `extract_pipeline_questions()` strips the questions section from text and returns cleaned text — use this before `parse_pipeline_stage_output()` to avoid the questions JSON being picked up as plan output
  - `run_pipeline_stage()` now returns a 3-tuple with raw result_text — all callers must destructure the third element (use `_` prefix if unused)
  - For parallel async tasks within an async block, use `tokio::spawn` + clone all necessary data into the moved closure
  - Manual mode question waiting uses DB polling (500ms interval) — simple and robust, avoids additional signal channel complexity
---
