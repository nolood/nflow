# Ralph Progress Log
Started: Wed Feb 11 2026
Branch: ralph/pipeline-interactive-mode
---

## Codebase Patterns
- PipelineRun struct fields must be updated in: nflow-core/src/pipeline.rs (struct + new()), db/pipeline.rs (row_to_pipeline_run, insert_pipeline_run, all SELECT queries), handlers.rs (all inline PipelineRun{} constructors and PipelineRun::new() calls)
- Clippy prefers `#[derive(Default)]` with `#[default]` attribute over manual `impl Default` for enums
- SELECT queries for pipeline_runs are replicated in: get_pipeline_run, list_pipeline_runs, get_active_pipeline_run — all must be updated together
- TUI uses its own PipelineRunItem display struct, not the core PipelineRun — adding fields to core doesn't require immediate TUI changes
- Migrations go in crates/nflow-daemon/migrations/ with numbered prefix (e.g., 004_pipeline_mode.sql)
- Branch should be created from `dev` (not `main`), since `main` only has initial docs commit

---

## 2026-02-11 - US-001
- What was implemented: PipelineMode enum (Manual/Auto) with Display, Default, Serialize, Deserialize; mode field on PipelineRun; updated PipelineRun::new() signature; DB helpers (pipeline_mode_to_str/from_str); updated row mapper and insert; migration 004_pipeline_mode.sql; updated all handler call sites
- Files changed:
  - crates/nflow-core/src/pipeline.rs (PipelineMode enum, mode field, updated new(), updated tests)
  - crates/nflow-daemon/src/db/pipeline.rs (mode helpers, updated row mapper, insert, all SELECT queries)
  - crates/nflow-daemon/src/handlers.rs (5 PipelineRun constructor sites updated with mode field)
  - crates/nflow-daemon/migrations/004_pipeline_mode.sql (new migration)
- **Learnings for future iterations:**
  - The handlers.rs file has 4 inline `PipelineRun { ... }` constructs plus 1 `PipelineRun::new()` call — all need updating when adding fields
  - The old prd.json from dev branch must be overwritten with the new PRD when creating a new branch
  - All 4 SELECT queries in db/pipeline.rs must include new columns (get, list, get_active + find_stages doesn't apply to pipeline_runs)
---
